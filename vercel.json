{
    "version": 2,
    "builds": [
        {
            "src": "api/config.js",
            "use": "@vercel/node"
        },
        {
            "src": "package.json",
            "use": "@vercel/static-build",
            "config": {
                "distDir": "."
            }
        }
    ],
    "routes": [
        {
            "src": "/api/config",
            "dest": "/api/config.js"
        },
        {
            "src": "/(.*)",
            "dest": "/$1"
        }
    ]
}