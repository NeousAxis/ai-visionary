---
description: Deploy the project to Vercel Production
---

This workflow deploys the current state of the project to Vercel Production.

1. Ensure all changes are saved.
// turbo
2. Run the Vercel deployment command:
   ```bash
   vercel --prod
   ```
